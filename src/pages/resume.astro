---
import Layout from '../layouts/Layout.astro';

const resumePath = "/Resume - Vincent Nguyen.pdf";
---

<Layout title="Resume" description="Vincent Nguyen's Resume">

  <section class="pb-8 border-b border-slate-100 dark:border-slate-800">
    <p class="text-blue-600 dark:text-blue-400 font-semibold text-sm tracking-widest uppercase mb-4">Resume</p>
    <h1 class="text-4xl sm:text-5xl font-extrabold text-slate-900 dark:text-slate-50 leading-tight mb-2">Things you've already read (but better formatted)</h1>
  </section>

  <div class="mt-10 relative rounded-2xl border border-slate-200 dark:border-slate-700 overflow-hidden bg-slate-50 dark:bg-slate-900 min-h-[700px]">
    <div id="pdf-placeholder" class="absolute inset-0 flex flex-col items-center justify-center text-center p-8 pointer-events-none">
      <div class="w-14 h-14 rounded-xl bg-slate-200 dark:bg-slate-700 flex items-center justify-center mb-4 text-2xl">📄</div>
      <p class="text-slate-500 dark:text-slate-400 text-sm max-w-sm leading-relaxed">
        Loading...
      </p>
    </div>
    <iframe
      src={resumePath}
      id="pdf-frame"
      title="Vincent Nguyen Resume"
      class="w-full h-[900px] block border-none opacity-0 transition-opacity duration-300"
    ></iframe>
  </div>

</Layout>

<script>
  const frame = document.getElementById('pdf-frame') as HTMLIFrameElement;
  const placeholder = document.getElementById('pdf-placeholder');

  if (frame && placeholder) {
    frame.addEventListener('load', () => {
      placeholder.style.display = 'none';
      frame.style.opacity = '1';
    });
  }
</script>
